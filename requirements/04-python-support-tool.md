# 04 – Python Support Utility
Build a Python CLI: `python support_tool.py --transaction TXN000123`.
Must return transaction details, status, anomalies, and recommended next actions. Support `--json` flag. Config via env/files, proper exit codes, and automated unit tests.
